# Video Pipeline — Progress Log

Durable memory for the agentic video-generation build. Append one line per checkpoint: what changed, tests run, result, next step.

---

- **2026-08-12 — Phase 0 discovery complete.** Ran read-only codebase audit (no files changed). Key finding: a **reel pipeline already exists** (`ShotstackService`, `ReelAssemblerService`, `ElevenLabsService`, `PixabayMusicService`, `video-assembly` BullMQ queue/worker) — this is a slideshow/reels render core we should extend, not rebuild. Blocking gaps identified: no Anthropic tool-use wrapper (agents must be built from scratch), no webhook receivers for external providers (Shotstack currently polls), no Runway integration, no TikTok publisher, feature-flag system is a frontend-only stub with no backend persistence. Tests: none (discovery only). Next: present findings to user at GATE, get sign-off before Phase 1 (Video Plan Prisma contract).

- **2026-08-12 — Phase 1 Video Plan contract complete.** Added `VideoType`, `SceneAssetKind`, `Motion`, `Transition`, `VideoStatus`, `CriticStatus` enums and a `VideoPlan` model to `backend/prisma/schema.prisma` (JSON-blob-plus-denormalized-columns pattern, matching `GenerationJob`/`ContentItem` convention); wired back-relations on `Tenant`, `Appointment`, `Client`, `User`, `BrandDNA`, `GenerationJob`, `ContentItem`. Built the single-source-of-truth zod schema + TS types (`backend/src/ai/video/video-plan.schema.ts`) and constants (`backend/src/ai/video/video-plan.constants.ts`). Tests: `npx prisma validate` clean, `npx prisma generate` clean, `video-plan.schema.spec.ts` 8/8 passing (accepts well-formed plan + defaults, rejects off-enum videoType/motion/objective/aspect, rejects empty scenes, rejects non-uuid technicianId), `tsc --noEmit` shows zero new errors (pre-existing missing-module errors only, unrelated to this change). Design call: enum casing kept lowercase snake_case to match existing Prisma convention rather than the spec's illustrative uppercase; no shared TS package exists between backend/frontend yet, flagged for Phase 8. Committed locally on `dev-prashant` (not pushed). Next: Phase 2 — deterministic core. Since Shotstack/ElevenLabs/Pixabay integrations already exist (`ShotstackService`, `ReelAssemblerService`), this phase is "map Video Plan → existing Shotstack render path + add a webhook receiver to replace polling" rather than building render submission from scratch.

- **2026-08-12 — Phase 2 deterministic core complete.** Built the render+webhook+publish core with zero agents/LLM calls. New: `video-plan-render.mapper.ts` (generic Video Plan → Shotstack edit JSON, N scenes, ken_burns/transitions/text overlays/brand strip/music+voiceover soundtrack), `video-render.service.ts` (submit-to-Shotstack + handle-callback, both against the typed Prisma client), `video-render.worker.ts` (BullMQ worker on new `video-render` queue), `video-webhook.controller.ts` + `video.module.ts` (Nest route `POST /video/webhooks/shotstack/:videoPlanId?token=`, registered in `app.module.ts`), `slideshow-plan-builder.ts` (trivial rule-based plan builder — proves the core works with zero agents, per the phase's de-risking goal). Reused `ShotstackService.submitRender()` as-is (added an optional `callback` field to its render-json type) and `PixabayMusicService` for music-mood→CDN-url resolution at render time. Publish reuse: webhook success syncs `outputUrl` into `ContentItem.finalVideoUrl`/`reelStatus` when a `contentItemId` is linked, so the existing Instagram publish path needs zero new code. Tests: 28/28 passing across 4 new spec files (`video-plan-render.mapper.spec.ts`, `video-render.service.spec.ts` — the required "submit → simulated webhook → RENDERED" integration test with Shotstack mocked, `slideshow-plan-builder.spec.ts`), `tsc --noEmit` clean (no new errors). **Not run:** a real end-to-end staging render (needs a live `SHOTSTACK_API_KEY` + publicly reachable `BACKEND_PUBLIC_URL` for the callback — outside this environment). **Discovered, not fixed:** `ReelAssemblerService`/`video-assembly.worker.ts` raw SQL references `content_item_id`/`reel_storage_path` columns that don't exist in `schema.prisma`'s `ContentItem` model — looks like pre-existing broken/dead code; the new Phase 2 core avoids it by going through the typed Prisma client instead, writing to the fields that actually exist (`finalVideoUrl`, `reelStatus`). Flagged for the team, out of scope to fix here. Env: added `BACKEND_PUBLIC_URL` and `VIDEO_WEBHOOK_SECRET` to `.env.example`; added `worker:video-render` npm script. Committed locally on `dev-prashant` (not pushed). Next: Phase 3 — agent runtime + Director + Script agent. This is the largest net-new build of the whole project since no Anthropic tool-use/function-calling wrapper exists anywhere in the codebase today (Phase 0 finding) — current LLM usage is all LangChain `ChatAnthropic` with plain-JSON-in-prompt parsing, not real tool-use.
